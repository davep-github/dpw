--- saveconf.el.ORIG	2005-05-02 09:45:05.000000000 -0400
+++ saveconf.el	2005-12-16 21:48:00.000000000 -0500
@@ -268,11 +268,13 @@
 	;;
 	;; Recover buffer contexts, if any.
 	;;
-	(while (setq sexpr (read context-buffer))
-	  (set-buffer (find-file-noselect sexpr t))
-	  (if (zerop (buffer-size))
-	      (kill-buffer (current-buffer))
-	    (goto-char (read context-buffer))))
+        (let (point)
+          (while (setq sexpr (read context-buffer))
+            (setq point (read context-buffer))
+            (set-buffer (find-file-noselect sexpr t))
+            (if (zerop (buffer-size))
+                (kill-buffer (current-buffer))
+              (goto-char point))))
 	(bury-buffer "*scratch*")
 	(kill-buffer context-buffer)
 	t )
