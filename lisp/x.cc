//new!!!! <:bof:>
/* -*- mode: c++; c-file-style: "ll-c-style" -*- */
//   What's happening to tramp-auto-save-directory?
// it's ok immediately after changing a tramp file. AHHH!
// change go force svn commit.
//
// {I {hope things} {are going {well}}}

#ifdef OLDoldOLD
/* -*- mode: c++; c-file-style: "av2-c-style" -*- */
#endif

// Some characters that usually look too similar or just plain crappy.
// () -- parens
// {} -- braces
// [] -- brackets
// 0  -- zero
// O  -- capital `Oh'
// ` ' -- back and single quotes.
// lllllll -- 7 `Els'
// mmmmmmm -- 7 `Ems'

typedef typename yaya yadda;

/*
 * aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
 * bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
 * ccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccc
 * ddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddd
 * eeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeee
 * 
/*
************************************************************************
*
* 
* 
************************************************************************
*/

   fab
00fab(
//

name_std::string
class myclass
{
    tmp_cout << x;              // whoop!
    tmp_cerr << z;
    // @tmp@
    assert(c);                  // @dbg@
                                //!<@todo fix this
    { //  @todo:debug:REMOVE 2006-03-11T12:36:16 
        aaa();
    } //  @todo:debug:REMOVE 2006-03-11T12:36:16 

    if (c < x) {                // @!!!@ is this right?
        boop('!');
    }

    int
    a_function_def(
        with,
        way,
        too, many, parameters,
        but,
        it,
        is,
        a,
        good, example, for,
        testing, the,
        stack,
        and,
        fill,
        statement,
        commands)
    {
        x(x),;
    }
    
    
    void
    f(
        int a)
    {
        a_function_call(with, way, too, many, parameters, but, it, is, a,
                        good, example, for, testing, the, stack, and, fill,
                        statement, commands);
        std::cout << a << std::endl;
        a_function_call(with, way, too, many, parameters, but, it, is, a,
                        good, example, for, testing, the, stack, and, fill,
                        statement, commands);
    }
    
    myclass(
        int a,
        int b,
        char* s) 
            :
            xa(a),
            xb(b),
            xs(s)
    {}
    
    myclass(
        int	v1) 
            :
            a(v1)
    {
        std::cout << "Wha!?" << std::endl;
    }
    
    std::string boo;
    
    my_string boo;
    string_holder bo2o;
    std::vector bii;
    
    
    myclass(
        int	v1) 
            :
            internal_v(v1)
    {
        blah();
        boo();
        boo();
        x();
        y();                        // woo-hoo?
        if (a) {
            yyy();
        } else {
            zzz();
        }
        if (v) {
            ya();
        }
        else {
            z();
        }
        
        if (q)
            doq();
        else {
            do_nonq();
        }
        
        if (qq) {
            doqq();
        } else {
            noqq();
        }
    }
    
    /*
     * if boo
     * then blah
     */
    
    
    /*********************************************************************/
    /*!
     * @brief a func in an afuncy way
     */
    int afunc(
        void)
    {
        blah();			//!<@todo fix this! 
    }
    
    
    /*********************************************************************/
    /*!
     * @brief 
     */
    void
    d4d(
        int	  count,		//!<@arg the count 
        int	  amount)		//!<@arg the amount
    {
        int	x;			// 
        
        // a one liner
        
        int z;			// another one liner
        int	q;			// boo
        // blah
        int aa;			// boo
        
        /*
         *  kdkd ldldl dldldl
         */
        
    }
    
    
    
    
    /*********************************************************************/
    /*!
     * @brief bfunc does b-like things in a b-funcy way
     */
    bfunc()
    {
        do_b_like_things();
    }
    
    // data
    int first_thing;		      //!<
    blah;
    int something;		      //!< something 
    int other;			      //!< other 
    
    
};



/*********************************************************************/
/*!
 * @brief rikes!
 */
fa()
{
    if (a>1 or a <2) {
        panic("");
    }
}

/*********************************************************************/
/*!
 * @brief f2 calls boog()
 */
f2()
{
    boog();
}

/*
************************************************************************
*
* ./x.hxx#tst_init
* 
************************************************************************
*/
int
tst_init(
    unsigned int	num_timers=8)
{
    xyz_t   blah;
    
    if (tst_timers)
        del [] tst_timers;
    
    tst_timers = new tst_timer_t[num_timers];
    if (tst_timers) {
        TST_NUM_TIMERS = num_timers;
        for (int i = 0; i < num_timers; i++)
            tst_init_timer(&tst_timers[i]);
    }
    else
        TST_NUM_TIMERS = 0;
    
    
    if (is_true) {
        strewth();
    } else {
        sbull();
    }
    
    if (boo)
        eek();
    else
        blah();
    
    if (blah)
        oo;
    else
        bb;
    
    std::endl;
    std::cout;
    
    
    
    cout << endl;
    
    
    
    a_long_var_name = aaa +
                      bbb +
                      ccc;
    
    function_aaa(boo,
                 doo,
                 daa);
    
    // booo
    
    /*
     * bloog
     * blowing
     * by
     * don't expand endl in comments.
     * endl
     * see, vector does not expand in a comment :->
     */
    
    std::cout;
    std::cout;
    std::endl;
    std::cout << std::endl;
    std::endl;
    std::endl;
    std::vector<boo>;

    int z;
    int     a;
    int         b;
    int             c;
    size_t      max_elements;
    int             max_elements;
    int a;
    int b;
    char    x;
    int     iz;
    
    
    // < blah >
    if (the_rain(inspain)) {
        fallsin(plain) ? "NO" : "YES!";
    } else {
        printf("Sorry Eliza.\n");
    }
    
    return (tst_timers == 0);	// fail if new failed
    /*
     * a longish comment.  does autofill work ok here?  let's see...
     * yup.
     */
    
}

fxx(void)
{
  if (aaa) {
    blah();
  }
  else {
  }
}

//!<@arg 
f11(void)
{
  if (x) {
    y()
  } else {
    break
  }

  f();
  
  return z;                     /* end? */
}

class blah

  
