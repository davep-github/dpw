# This, ideally, can be [include]'d first and things can be added or
# overridden.

[include]
        path = ~/.rc/gitconfig.diff-n-merge-tools

[include]
        path = ~/.rc/gitconfig.aliases

[include]
        path = ~/.rc/gitconfig.nice-defaults

[color]
	ui = auto

[push]
	disabled = false
