# OBSOLETE
#set -x
if tty -s
then
	tty=`tty`
	tty=`basename $tty`
	TTY=${TTY:-$tty}
fi

. $DP_RC_DIR/alias
. $DP_RC_DIR/func

[ -f $DP_RC_DIR/rc.${HOST} ] && . $DP_RC_DIR/rc.${HOST}

cd .				# ensure activation of prompt/titler

true
