SVNROOTWORK=$PROJECT_LOCATION
# ??? Why did I have this commented out?
EXTRA_RCS=$($PATHADDBIN -S "$EXTRA_RCS" "$PROJECT")

# Lazy... do it here
export GOPATH=$($PATHADDBIN -p "$GOPATH" "$HOME/.go$PROJECT" "$HOME/.go.$PROJECT_LOCATION")

true
