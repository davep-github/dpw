# -*- mode: conf -*-

[core]
	excludesfile = ~/.gitignore

[color]
        ui = auto

[merge]
	keepBackup = true;
        summary = true
        verbosity = 2

[push]
        default = simple

[merge]
        summary = true

[diff]
        # Git diff will use (i)ndex, (w)ork tree, (c)ommit and (o)bject
        # instead of a/b/c/d as prefixes for patches
        mnemonicprefix = true

[grep]
        patternType = perl

# Is there more advicey advice?
#[advice]
#  statusHints = false
