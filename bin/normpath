#!/usr/bin/env python

import sys, os

if __name__ == "__main__":
    path_ops = [os.path.normpath]
    argv = sys.argv[1:]
    if argv[0] == '-r':
        argv = argv[1:]
        path_ops.append(os.path.realpath)
    for p in argv:
        for op in path_ops:
            p = op(p)
        print p
