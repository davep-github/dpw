#!/bin/sh

source script-x
set -u
progname="$(basename $0)"

: ${IPYTHON_HOME:=/home/utils/ipython-0.13/}
: ${IPYTHON_PYTHON:=python27}

PYTHONPATH=$(pathadd "$PYTHONPATH" "$IPYTHON_HOME")

cd "$IPYTHON_HOME"
exec "$IPYTHON_PYTHON" ./ipython.py
