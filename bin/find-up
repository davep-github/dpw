#!/usr/bin/env python

import sys, os

def main(args):
    import getopt
    highest_p = False
    opts, args = getopt.getopt(sys.argv[1:], 'h')
    for o, v in opts:
        if o == '-h':
            highest_p = not highest_p
            continue

if __name__ == "__main__":
    main(sys.argv)




