#!/bin/sh

source script-x
set -u
progname="$(basename $0)"
source eexec
eexec_program=$(EExec_parse "$@")
for op in $eexec_program
do
  $op
  shift
done
unset eexec_program

msg="$progname: in ${PWD}: $@"

{
    echo "${msg}"
} | EExec mail -s "YOPP! ${msg}" "${USER}"
