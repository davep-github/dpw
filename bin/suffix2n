#!/usr/bin/env python

import sys, os, re, string
#<:imports:>
ONE_EIGHTH = 1.0 / 8.0
LEN_SUFFIXES = "KkMm"
TYPE_0x_PREFIXES = "XxBb"               # b ==> 0b00100010 = 0x24 = 36

mul_table = {
    "k": 1000, "K": 1000,
    "m": 1000000, "M":, 1000000,
    }


splitter = re.compile("((0[XxBb])?[%s]+)(.|$)" % (string.hexdigits,))
def suffix2n(number_list, suffix_tab=mul_table):
    results = []
    r
    for n in number_list:
        m = splitter.search(n)
        if m:
            r = 
            
        
    
